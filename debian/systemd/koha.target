[Unit]
Description=Koha ILS - All instances
After=network.target memcached.service mysql.service
# Stopping or restarting the koha-common compat unit takes the whole stack
# with it, the way 'service koha-common' did under SysV.
PartOf=koha-common.service

[Install]
WantedBy=multi-user.target
