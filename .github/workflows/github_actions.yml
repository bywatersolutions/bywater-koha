name: Unit Tests
on:
    push:
        branches:
            - "**"
        tags-ignore:
            - "**"
    pull_request_target:
      types: [opened, reopened, synchronize]
    workflow_dispatch:
        inputs:
          debug:
            description: 'Enable debug session (true/false)'
            required: false
            default: 'false'
env:
    DEBUG_SESSION: ${{github.event.input.debug}}
jobs:
    check-commit-messages:
        name: Check PR commit messages start with "BWS-PKG - "
        if: github.event_name == 'pull_request_target'
        runs-on: ubuntu-latest
        steps:
            - name: Verify all commits start with "BWS-PKG - "
              env:
                  GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
                  PR_NUMBER: ${{ github.event.pull_request.number }}
                  REPO: ${{ github.repository }}
              run: |
                  set -e
                  bad=$(gh api --paginate "repos/$REPO/pulls/$PR_NUMBER/commits" \
                      --jq '.[] | select(.commit.message | startswith("BWS-PKG - ") | not) | "\(.sha[0:7]) \(.commit.message | split("\n")[0])"')
                  if [ -n "$bad" ]; then
                      echo "::error::The following commits do not start with 'BWS-PKG - ':"
                      echo "$bad"
                      exit 1
                  fi
                  echo "All commits start with 'BWS-PKG - '"

    check-repo:
        name: Check for correct repo
        runs-on: ubuntu-latest
        steps:
            - name: Get repository name
              run: |
                if [[ "${{ github.event_name }}" == "pull_request_target" ]]; then
                  echo "Skipping repo/version check for pull request"
                  exit 0
                fi
                if [[ "$GITHUB_REPOSITORY" == "bywatersolutions/bywater-koha-security" ]]; then
                  echo "Skipping execution for $GITHUB_REPOSITORY"
                  exit 0
                fi
                if [[ "$GITHUB_REPOSITORY" == "bywatersolutions/bywater-koha-devel" ]]; then
                  echo "Skipping execution for $GITHUB_REPOSITORY"
                  exit 0
                fi
                echo "Branch: ${{ github.ref_name }}"
                REPO_NAME="${{ github.repository }}"
                REPO_NAME=$(echo "$REPO_NAME" | cut -d'/' -f2)
                echo "$REPO_NAME"
                echo "REPO NAME: $REPO_NAME"
                URL="https://raw.githubusercontent.com/bywatersolutions/bywater-koha-versions/main/$REPO_NAME"
                echo "URL: $URL"
                curl -o version.txt $URL
                CORRECT_KOHA_VERSION=$(cat version.txt)
                echo "CORRECT KOHA VERSION: $CORRECT_KOHA_VERSION" 
                if [[ "${{ github.ref_name }}" == *"$CORRECT_KOHA_VERSION"* ]]; then
                  echo "Koha version pushed matches the current Koha version for repo $REPO_NAME"
                  exit 0
                else
                  echo "Koha version pushed *does not match* the current Koha version for repo $REPO_NAME"
                  exit 1
                fi

    is-a-mark:
        if: "contains(github.event.head_commit.message, ' - Set bwsbranch to')"
        name: Check for mark
        needs: [check-repo]
        runs-on: ubuntu-latest
        steps:
            - name: Print commit message
              run: |
                  echo "Commit Message:: ${{ github.event.head_commit.message }}"

    has-ncip-version:
        name: Has a matching NCIP server version
        needs: [check-repo]
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v1

            - name: Export additional variables
              id: extracted_variables
              run: |
                  # Export VERSION as Koha major and minor e.g. 18.11, 20.05, etc.. TODO: Make this into a js based github action.
                  export FULL_VERSION="$(cat Koha.pm | grep '$VERSION =')" && export FULL_VERSION=${FULL_VERSION%\"*} && export FULL_VERSION=${FULL_VERSION##*\"} && echo $FULL_VERSION
                  IFS='.' read -ra VER_PARTS <<< "$FULL_VERSION"
                  KOHA_MAJOR=${VER_PARTS[0]}
                  KOHA_MINOR=${VER_PARTS[1]}
                  MAJOR_MINOR_VERSION="${KOHA_MAJOR}.${KOHA_MINOR}"
                  echo "MAJOR_MINOR_VERSION: $MAJOR_MINOR_VERSION"
                  echo ::set-output name=MAJOR_MINOR_VERSION::$MAJOR_MINOR_VERSION

            - name: Check to see if there is a matching NCIP server tag on Docker Hub
              run: |
                  curl --silent "https://registry.hub.docker.com/v2/repositories/bywater/koha-ncip-server/tags?page_size=1000" | jq -r ".results[].name" | sort --version-sort | grep "v${{ steps.extracted_variables.outputs.MAJOR_MINOR_VERSION }}-latest"

    unit-test-perl:
        # Fast builds will skip unit tests and go straight to building a debian package
        if: (!contains(github.event.head_commit.message, '[FAST]')) && (github.repository == 'bywatersolutions/bywater-koha' || github.repository == 'bywatersolutions/bywater-koha-future' || github.repository == 'bywatersolutions/bywater-koha-security' || github.repository == 'bywatersolutions/bywater-koha-devel')
        name: Run Perl unit tests (${{ matrix.koha_image }} shard ${{ matrix.shard.name }})
        needs: [check-repo]
        runs-on: ubuntu-latest
        strategy:
            fail-fast: false
            matrix:
                # Every shard runs on both OS bases we support: Debian 12 ( bookworm,
                # Perl 5.36 ) and Debian 13 ( trixie, Perl 5.40 ). The trixie cells guard
                # against the Perl 5.40 import-method warning regression ( Koha bug 41077 ):
                # a custom module that imports from a not-yet-loaded package warns under
                # 5.40, and our Test::NoWarnings-based tests fail when it does.
                koha_image: [25.11-bookworm, 25.11-trixie]
                # Four es8 shards hold the bulk of the suite. A separate Zebra shard runs
                # the few tests that drive the live search UI and need Zebra ( they use
                # t::lib::Mocks::Zebra ), which can't share an es8 instance with the rest.
                shard:
                    - { name: "0", engine: es8, index: 0, total: 4 }
                    - { name: "1", engine: es8, index: 1, total: 4 }
                    - { name: "2", engine: es8, index: 2, total: 4 }
                    - { name: "3", engine: es8, index: 3, total: 4 }
                    - { name: zebra, engine: zebra, index: 0, total: 4 }
        steps:
            - uses: actions/checkout@v1

            - name: Export additional variables needed by koha-testing-docker
              run: |
                  KOHA_BRANCH=${GITHUB_REF##*/}
                  echo "BRANCH: $KOHA_BRANCH"
                  echo "KOHA_BRANCH=$KOHA_BRANCH" >> $GITHUB_ENV
                  echo "KOHA_IMAGE=${{ matrix.koha_image }}" >> $GITHUB_ENV
                  # misc4dev reads debian/koha-common.install, which bug 40901 removes.
                  # Until koha-misc4dev MR !92 is merged, KTD can't start on this branch.
                  echo "DEBUG_GIT_REPO_MISC4DEV=yes" >> $GITHUB_ENV
                  echo "DEBUG_GIT_REPO_MISC4DEV_URL=https://gitlab.com/koha-community/koha-misc4dev.git" >> $GITHUB_ENV
                  echo "DEBUG_GIT_REPO_MISC4DEV_BRANCH=issue_105" >> $GITHUB_ENV

            - name: Setup Debug Session
              if: env.DEBUG_SESSION == 'true'
              uses: csexton/debugger-action@master

            - name: Run tests
              env:
                  SHARD: ${{ matrix.shard.name }}
                  ENGINE: ${{ matrix.shard.engine }}
                  SHARD_INDEX: ${{ matrix.shard.index }}
                  SHARD_TOTAL: ${{ matrix.shard.total }}
              run: |
                  sudo apt-get update
                  sudo apt-get install -y cpanminus
                  sudo cpanm Modern::Perl IPC::Cmd

                  # Tests excluded from the shards. These are infrastructure/environment
                  # limitations, not customization failures, and they currently run nowhere
                  # else ( there is no full-suite job ) -- revisit if that changes:
                  #   - search_utf8.t restarts Plack itself, which fails against a live KTD.
                  #   - the selenium tests below are flaky here / need a browser.
                  # ( t/00-check-atomic-updates.t is NOT listed: it self-skips because the
                  #   test run sets CUSTOM_PACKAGE=1 -- see koha_tests_runner.pl. )
                  EXCLUDE=$(printf '%s\n' \
                      't/db_dependent/www/search_utf8.t' \
                      't/db_dependent/selenium/basic_workflow.t' \
                      't/db_dependent/selenium/patrons_search.t' \
                      't/db_dependent/selenium/system_preferences_search.t')

                  # Tests that drive the live search UI and rely on Zebra ( they use
                  # t::lib::Mocks::Zebra ). They run in a dedicated Zebra shard; every es8
                  # shard excludes them so no test runs against the wrong search engine.
                  ZEBRA_TESTS=$(grep -rl 't::lib::Mocks::Zebra' t --include='*.t' | sort)

                  if [ "$SHARD" = "zebra" ]; then
                      FILES=$(printf '%s\n' "$ZEBRA_TESTS" | grep -vxF -f <(printf '%s\n' "$EXCLUDE") | tr '\n' ' ')
                      ENGINE_ARG="--search-engine zebra"
                  else
                      # Everything except the Zebra set and the excluded tests, round-robin across the es8 shards
                      FILES=$(find t -name '*.t' | sort \
                          | grep -vxF -f <(printf '%s\n' "$ZEBRA_TESTS") \
                          | grep -vxF -f <(printf '%s\n' "$EXCLUDE") \
                          | awk -v t="$SHARD_TOTAL" -v s="$SHARD_INDEX" 'NR % t == s' | tr '\n' ' ')
                      ENGINE_ARG=""
                  fi

                  # Run the fast xt security/correctness checks once, on the first es8 shard.
                  # They scan the tree ( no DB needed ) and guard our custom code against
                  # missing output filters, auth checks, CSRF tokens and form op fields.
                  if [ "$SHARD" != "zebra" ] && [ "$SHARD_INDEX" = "0" ]; then
                      FILES="$FILES xt/find-missing-filters.t xt/find-missing-auth_checks.t xt/find-missing-csrf.t xt/find-missing-op-in-forms.t"
                  fi

                  echo "Excluded from the shards ( not run anywhere ): $(echo $EXCLUDE | tr '\n' ' ')"
                  echo "Shard '${SHARD}' (engine ${ENGINE}) -- $(echo $FILES | wc -w) test files:"
                  echo "$FILES" | tr ' ' '\n'

                  perl .github/workflows/koha_tests_runner.pl $ENGINE_ARG --files "$FILES"

            - uses: bywatersolutions/action-slack@v3
              with:
                status: ${{ job.status }}
                fields: ref,workflow # selectable (default: repo,message)
              env:
                SLACK_WEBHOOK_URL: ${{ secrets.SLACK_DEVOPS_WEBHOOK }} # required
              if: failure() # Only notify on job failures.

    unit-test-cypress:
        # Fast builds will skip unit tests and go straight to building a debian package
        if: (!contains(github.event.head_commit.message, '[FAST]')) && (github.repository == 'bywatersolutions/bywater-koha' || github.repository == 'bywatersolutions/bywater-koha-future' || github.repository == 'bywatersolutions/bywater-koha-security' || github.repository == 'bywatersolutions/bywater-koha-devel')
        name: Run Cypress tests (${{ matrix.koha_image }})
        needs: [check-repo]
        runs-on: ubuntu-latest
        strategy:
            fail-fast: false
            matrix:
                # Cypress is frontend/JS, so the OS base rarely matters, but run both Debian
                # 12 ( bookworm ) and Debian 13 ( trixie ) to catch node / asset / browser drift.
                koha_image: [25.11-bookworm, 25.11-trixie]
        steps:
            - uses: actions/checkout@v1

            - name: Export additional variables needed by koha-testing-docker
              run: |
                  KOHA_BRANCH=${GITHUB_REF##*/}
                  echo "BRANCH: $KOHA_BRANCH"
                  echo "KOHA_BRANCH=$KOHA_BRANCH" >> $GITHUB_ENV
                  echo "KOHA_IMAGE=${{ matrix.koha_image }}" >> $GITHUB_ENV
                  # misc4dev reads debian/koha-common.install, which bug 40901 removes.
                  # Until koha-misc4dev MR !92 is merged, KTD can't start on this branch.
                  echo "DEBUG_GIT_REPO_MISC4DEV=yes" >> $GITHUB_ENV
                  echo "DEBUG_GIT_REPO_MISC4DEV_URL=https://gitlab.com/koha-community/koha-misc4dev.git" >> $GITHUB_ENV
                  echo "DEBUG_GIT_REPO_MISC4DEV_BRANCH=issue_105" >> $GITHUB_ENV

            - name: Setup Debug Session
              if: env.DEBUG_SESSION == 'true'
              uses: csexton/debugger-action@master

            - name: Run tests
              run: |
                  sudo apt-get update
                  sudo apt-get install -y cpanminus
                  sudo cpanm Modern::Perl IPC::Cmd
                  perl .github/workflows/koha_tests_runner.pl --run-cypress-tests-only

            - uses: bywatersolutions/action-slack@v3
              with:
                status: ${{ job.status }}
                fields: ref,workflow # selectable (default: repo,message)
              env:
                SLACK_WEBHOOK_URL: ${{ secrets.SLACK_DEVOPS_WEBHOOK }} # required
              if: failure() # Only notify on job failures.

    dbic-check:
        # Regenerate Koha/Schema with dbic and fail if kohastructure.sql drift produces changes
        if: (!contains(github.event.head_commit.message, '[FAST]')) && (github.repository == 'bywatersolutions/bywater-koha' || github.repository == 'bywatersolutions/bywater-koha-future' || github.repository == 'bywatersolutions/bywater-koha-security' || github.repository == 'bywatersolutions/bywater-koha-devel')
        name: Check DBIx::Class schema is up-to-date
        needs: [check-repo]
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v1

            - name: Export variables needed by koha-testing-docker
              run: |
                  echo "KOHA_IMAGE=25.11" >> $GITHUB_ENV
                  echo "LOCAL_USER_ID=$(id -u)" >> $GITHUB_ENV
                  echo "SYNC_REPO=$GITHUB_WORKSPACE" >> $GITHUB_ENV
                  # misc4dev reads debian/koha-common.install, which bug 40901 removes.
                  # Until koha-misc4dev MR !92 is merged, KTD can't start on this branch.
                  echo "DEBUG_GIT_REPO_MISC4DEV=yes" >> $GITHUB_ENV
                  echo "DEBUG_GIT_REPO_MISC4DEV_URL=https://gitlab.com/koha-community/koha-misc4dev.git" >> $GITHUB_ENV
                  echo "DEBUG_GIT_REPO_MISC4DEV_BRANCH=issue_105" >> $GITHUB_ENV

            - name: Clone koha-testing-docker
              run: |
                  git clone --branch main --single-branch --depth 1 https://gitlab.com/koha-community/koha-testing-docker.git "$HOME/ktd"
                  echo "KTD_HOME=$HOME/ktd" >> $GITHUB_ENV

            - name: Ensure proxy network exists
              run: |
                  docker network inspect proxy >/dev/null 2>&1 || docker network create proxy

            - name: Start koha-testing-docker
              run: |
                  export PATH=$KTD_HOME/bin:$PATH
                  cp $KTD_HOME/env/defaults.env $KTD_HOME/.env
                  echo "DEBUG_GIT_REPO_MISC4DEV=yes" >> $KTD_HOME/.env
                  echo "DEBUG_GIT_REPO_MISC4DEV_URL=https://gitlab.com/koha-community/koha-misc4dev.git" >> $KTD_HOME/.env
                  echo "DEBUG_GIT_REPO_MISC4DEV_BRANCH=issue_105" >> $KTD_HOME/.env
                  ktd --proxy --name dbic-check --search-engine zebra pull -q
                  ktd --proxy --name dbic-check --search-engine zebra up -d
                  ktd --name dbic-check --wait-ready 300

            - name: Run dbic and check for schema drift
              run: |
                  export PATH=$KTD_HOME/bin:$PATH
                  ktd --name dbic-check --shell --run "/kohadevbox/bin/dbic"
                  if [ -n "$(git status --porcelain)" ]; then
                      echo "::error::Running dbic produced file changes. kohastructure.sql and Koha/Schema/ have drifted. Run dbic locally and commit the regenerated schema files."
                      echo "--- git status ---"
                      git status
                      echo "--- git diff (tracked) ---"
                      git diff
                      echo "--- untracked files ---"
                      git ls-files --others --exclude-standard
                      exit 1
                  fi
                  echo "No DBIx::Class schema drift detected."

            - name: Stop koha-testing-docker
              if: always()
              run: |
                  export PATH=$KTD_HOME/bin:$PATH
                  ktd --name dbic-check down || true

            - uses: bywatersolutions/action-slack@v3
              with:
                status: ${{ job.status }}
                fields: ref,workflow
              env:
                SLACK_WEBHOOK_URL: ${{ secrets.SLACK_DEVOPS_WEBHOOK }}
              if: failure()

    release-notes: # This container checks out the branch on it's own, so no need for the GitHub checkout action
        if: github.repository == 'bywatersolutions/bywater-koha' || github.repository == 'bywatersolutions/bywater-koha-security'
        name: Build release notes
        needs: [build-debs, is-a-mark]
        runs-on: ubuntu-latest
        steps:
            - name: Export additional variables needed by bywater-koha-custom-rebaser
              run: |
                  KOHA_BRANCH=${GITHUB_REF##*/}
                  echo "KOHA_BRANCH: $KOHA_BRANCH"
                  echo "KOHA_BRANCH=$KOHA_BRANCH" >> $GITHUB_ENV

            - name: Run kylemhall/bywater-koha-release-notes-generator
              run: |
                  echo "BRANCH: $KOHA_BRANCH"
                  docker run --env DEBUG=1 --env UPLOAD=1 --env GITHUB_TOKEN=${{ secrets.OUR_GITHUB_TOKEN }} --env KOHA_BRANCH=$KOHA_BRANCH quay.io/bywatersolutions/bywater-koha-release-notes-generator

    release-notes-notify-general: # Message #general only for bywater releases
        if: contains(github.ref, 'bywater-v')
        name: Notify general channel about new release notes
        needs: release-notes
        runs-on: ubuntu-latest
        steps:
            - name: Export additional variables
              id: extracted_variables
              run: |
                  KOHA_BRANCH=${GITHUB_REF##*/}
                  echo "KOHA_BRANCH: $KOHA_BRANCH"
                  echo ::set-output name=KOHA_BRANCH::$KOHA_BRANCH
                  echo "KOHA_BRANCH=$KOHA_BRANCH" >> $GITHUB_ENV

            - name: Notify Slack
              run: |
                  curl -X POST -H 'Content-type: application/json' --data '{ "blocks": [{"type": "section","text": {  "type": "mrkdwn","text": "@here, new release notes are here! https://github.com/bywatersolutions/bywater-koha-release-notes/blob/master/${{ steps.extracted_variables.outputs.KOHA_BRANCH }}.md"}}]}' ${{ secrets.SLACK_GENERAL_WEBHOOK }}

    release-notes-notify-slack-channel: # Message #release-notes for all codebases
        name: Notify release-notes channel about new release notes
        needs: release-notes
        runs-on: ubuntu-latest
        steps:
            - name: Export additional variables
              id: extracted_variables
              run: |
                  KOHA_BRANCH=${GITHUB_REF##*/}
                  echo "KOHA_BRANCH: $KOHA_BRANCH"
                  echo ::set-output name=KOHA_BRANCH::$KOHA_BRANCH
                  echo "KOHA_BRANCH=$KOHA_BRANCH" >> $GITHUB_ENV

            - name: Notify Slack
              run: |
                  curl -X POST -H 'Content-type: application/json' --data '{ "blocks": [{"type": "section","text": {  "type": "mrkdwn","text": "@here, new release notes are here! https://github.com/bywatersolutions/bywater-koha-release-notes/blob/master/${{ steps.extracted_variables.outputs.KOHA_BRANCH }}.md"}}]}' ${{ secrets.SLACK_RELEASE_NOTES_WEBHOOK }}

    auto-rebase:
        if: startsWith( github.ref, 'refs/heads/bywater-v' ) && github.repository == 'bywatersolutions/bywater-koha'
        name: Rebase custom codebases
        needs: [build-debs, is-a-mark]
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v1

            - name: Export additional variables needed by bywater-koha-custom-rebaser
              id: extracted_variables
              run: |
                  KOHA_BRANCH=${GITHUB_REF##*/}
                  echo "KOHA_BRANCH: $KOHA_BRANCH"
                  echo ::set-output name=KOHA_BRANCH::$KOHA_BRANCH
                  export KOHACLONE="$(pwd)" # Needed for koha-testing-docker
                  echo "KOHACLONE: $KOHACLONE"
                  echo ::set-output name=KOHACLONE::$KOHACLONE

            - name: Run kylemhall/bywater-koha-custom-rebaser
              run: |
                  docker run --mount type=bind,source=${{ steps.extracted_variables.outputs.KOHACLONE }},target=/kohaclone -e DO_IT=1 -e GITHUB_TOKEN=$OUR_GITHUB_TOKEN -e KOHA_BRANCH=${{ steps.extracted_variables.outputs.KOHA_BRANCH }} quay.io/bywatersolutions/bywater-koha-custom-rebaser
              env:
                  OUR_GITHUB_TOKEN: ${{ secrets.OUR_GITHUB_TOKEN }}

            - uses: bywatersolutions/action-slack@v3
              with:
                status: ${{ job.status }}
                fields: ref,workflow # selectable (default: repo,message)
              env:
                SLACK_WEBHOOK_URL: ${{ secrets.SLACK_DEVOPS_WEBHOOK }} # required
              if: failure() # Only notify on job failures.

    build-debs:
        # Fast builds will skip unit tests and go straight to building a debian package
        # If it is fast build, we still want to honor cancellation and stop on failures
        if: ( success() || ( contains(github.event.head_commit.message, '[FAST]') && !cancelled() && !contains(needs.*.result, 'failure') ) ) && (github.repository == 'bywatersolutions/bywater-koha' || github.repository == 'bywatersolutions/bywater-koha-future' || github.repository == 'bywatersolutions/bywater-koha-security')
        name: Build Debian packages
        needs: [unit-test-perl, is-a-mark, dbic-check]
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v1

            - name: Export additional variables
              id: extracted_variables
              run: |
                  # Export VERSION as Koha major and minor e.g. 18.11, 20.05, etc.. TODO: Make this into a js based github action.
                  export FULL_VERSION="$(cat Koha.pm | grep '$VERSION =')" && export FULL_VERSION=${FULL_VERSION%\"*} && export FULL_VERSION=${FULL_VERSION##*\"} && echo $FULL_VERSION
                  IFS='.' read -ra VER_PARTS <<< "$FULL_VERSION"
                  KOHA_MAJOR=${VER_PARTS[0]}
                  KOHA_MINOR=${VER_PARTS[1]}
                  MAJOR_MINOR_VERSION="${KOHA_MAJOR}.${KOHA_MINOR}"
                  echo "MAJOR_MINOR_VERSION: $MAJOR_MINOR_VERSION"
                  echo ::set-output name=MAJOR_MINOR_VERSION::$MAJOR_MINOR_VERSION
                  # Export kohaclone path
                  KOHACLONE="$(pwd)"
                  echo "KOHACLONE: $KOHACLONE"
                  echo ::set-output name=KOHACLONE::$KOHACLONE
                  # Export current branch name
                  KOHA_BRANCH=${GITHUB_REF##*/}
                  echo "KOHA_BRANCH: $KOHA_BRANCH"
                  echo ::set-output name=KOHA_BRANCH::$KOHA_BRANCH
                  # Split the branch name into parts ( e.g. bywater-v19.05.01-03 => "bywater", "v19.05.01", "03 )
                  IFS='-' read -ra BRANCH_PARTS <<< "$KOHA_BRANCH"
                  # Extract shortname branch name
                  SHORTNAME=${BRANCH_PARTS[0]}
                  echo "SHORTNAME: $SHORTNAME"
                  echo ::set-output name=SHORTNAME::$SHORTNAME
                  # Extract version without v
                  TMP_VERSION=${BRANCH_PARTS[1]}
                  MMM_VERSION=${TMP_VERSION:1} # Removes the first character i.e. 'v19.05.01' => '19.05.01'
                  echo "MMM_VERSION: $MMM_VERSION"
                  echo ::set-output name=MMM_VERSION::$MMM_VERSION
                  # Extract mark
                  MARK=${BRANCH_PARTS[2]}
                  echo "MARK: $MARK"
                  echo ::set-output name=MARK::$MARK
                  # Generate tag name
                  TAG_NAME="${SHORTNAME}_v${MMM_VERSION}-${MARK}"
                  echo "TAG NAME: $TAG_NAME"
                  echo ::set-output name="TAG_NAME::$TAG_NAME"

            - name: Echo KOHA_BRANCH from previous step
              run: echo "${{ steps.extracted_variables.outputs.KOHA_BRANCH }}"

            - name: Create dir for debian packages
              run: mkdir /tmp/debs

            - name: Clean out old docker images, just in case
              run: docker system prune -a

            - name: Run koha-dpkg-docker
              run: |
                  docker run --volume=${{ steps.extracted_variables.outputs.KOHACLONE }}:/koha --volume=/tmp/debs:/debs --privileged --env VERSION="${{ steps.extracted_variables.outputs.MMM_VERSION }}~${{ steps.extracted_variables.outputs.SHORTNAME }}~${{ steps.extracted_variables.outputs.MARK }}" --env RELEASE="${{ steps.extracted_variables.outputs.MAJOR_MINOR_VERSION }}" --env DEB_BUILD_OPTIONS="nocheck" koha/koha-dpkg:main

            - name: List file in debs dir
              run: ls -alh /tmp/debs

            - name: Stash koha-common deb file name
              id: find_koha_common
              run: |
                  KOHA_COMMON_DEB=$(ls /tmp/debs | grep koha-common)
                  echo "KOHA COMMON DEB: $KOHA_COMMON_DEB"
                  echo ::set-output name=KOHA_COMMON_DEB::$KOHA_COMMON_DEB

            - name: Create artifact zip
              run: zip --junk-paths -r "/tmp/${{ steps.extracted_variables.outputs.KOHA_BRANCH }}.zip" /tmp/debs/*

            - name: List contents of current directory, should contain artifacts zip
              run: ls -alh

            - name: Create Release
              id: create_release
              uses: bywatersolutions/github-action-create-release@master
              env:
                  GITHUB_TOKEN: ${{ secrets.OUR_GITHUB_TOKEN }} # This token is provided by Actions, you do not need to create your own token
              with:
                  tag_name: ${{ steps.extracted_variables.outputs.TAG_NAME }}
                  release_name: Release ${{ steps.extracted_variables.outputs.KOHA_BRANCH }}
                  target: ${{ steps.extracted_variables.outputs.KOHA_BRANCH }}
                  draft: false
                  prerelease: false

            - name: Upload assets
              uses: softprops/action-gh-release@v1
              with:
                tag_name: ${{ steps.extracted_variables.outputs.TAG_NAME }}
                files: |
                  /tmp/${{ steps.extracted_variables.outputs.KOHA_BRANCH }}.zip
                  /tmp/debs/${{ steps.find_koha_common.outputs.KOHA_COMMON_DEB }}
              env:
                GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

            - uses: bywatersolutions/action-slack@v3
              with:
                status: ${{ job.status }}
                fields: ref,workflow # selectable (default: repo,message)
              env:
                SLACK_WEBHOOK_URL: ${{ secrets.SLACK_DEVOPS_WEBHOOK }} # required
              if: failure() # Only notify on job failures.

            - uses: bywatersolutions/action-slack@v3
              with:
                status: ${{ job.status }}
                fields: ref,workflow # selectable (default: repo,message)
              env:
                SLACK_WEBHOOK_URL: ${{ secrets.SLACK_KYLE_WEBHOOK }} # required
              if: always() # Pick up events even if the job fails or is canceled.

            - uses: bywatersolutions/action-slack@v3
              with:
                status: custom
                fields: all
                custom_payload: |
                  {
                    username: 'GitHub Koha Package Notifier',
                    icon_emoji: ':octocat:',
                    attachments: [{
                      color: '${{ job.status }}' === 'success' ? 'good' : '${{ job.status }}' === 'failure' ? 'danger' : 'warning',
                      title: "New Koha Debian Package Ready!",
                      text: `The Koha Debian package for branch ${process.env.AS_REF} has been built and is ready to import into Aptly!`,
                    }]
                  }
              env:
                SLACK_WEBHOOK_URL: ${{ secrets.SLACK_KYLE_WEBHOOK }}

            - name: Sleep for 30 seconds
              run: sleep 30s
              shell: bash

            - name: Notify package importer
              run: |
                curl --insecure --fail -w "\nHTTP Response Code: %{http_code}\n" "https://apt2.bywatersolutions.com/?tag=${{ steps.extracted_variables.outputs.TAG_NAME }}&t=${{ secrets.KOHA_IMPORT_GITHUB_RELEASES_TOKEN }}"
              shell: bash


    lock-release-branch:
        name: Lock release branch after successful build
        needs: [build-debs]
        if: |
            success() &&
            startsWith(github.event.head_commit.message, 'BWS-PKG - Set bwsbranch to') &&
            (github.repository == 'bywatersolutions/bywater-koha' ||
             github.repository == 'bywatersolutions/bywater-koha-future' ||
             github.repository == 'bywatersolutions/bywater-koha-security')
        runs-on: ubuntu-latest
        steps:
            - name: Mint GitHub App token
              id: app-token
              uses: actions/create-github-app-token@v1
              with:
                  app-id: ${{ secrets.BRANCH_LOCK_APP_ID }}
                  private-key: ${{ secrets.BRANCH_LOCK_APP_PRIVATE_KEY }}

            - name: Create ruleset to lock branch
              env:
                  GH_TOKEN: ${{ steps.app-token.outputs.token }}
                  BRANCH: ${{ github.ref_name }}
                  REPO: ${{ github.repository }}
              run: |
                  set -e
                  cat > /tmp/ruleset.json <<EOF
                  {
                    "name": "lock-${BRANCH}",
                    "target": "branch",
                    "enforcement": "active",
                    "conditions": {
                      "ref_name": {
                        "include": ["refs/heads/${BRANCH}"],
                        "exclude": []
                      }
                    },
                    "rules": [
                      {"type": "update"},
                      {"type": "deletion"},
                      {"type": "non_fast_forward"}
                    ],
                    "bypass_actors": []
                  }
                  EOF
                  gh api --method POST "repos/${REPO}/rulesets" --input /tmp/ruleset.json


    build-docker:
        if: false #github.repository == 'bywatersolutions/bywater-koha' || github.repository == 'bywatersolutions/bywater-koha-future'
        name: Build Docker image
        needs: build-debs
        runs-on: ubuntu-latest
        steps:
            - name: Git clone koha-docker
              run: git clone https://gitlab.com/koha-community/docker/koha-docker.git --depth 1 --branch master

            - name: Export additional variables
              id: extracted_variables
              run: |
                  # Export VERSION as Koha major and minor e.g. 18.11, 20.05, etc.. TODO: Make this into a js based github action.
                  # Export current branch name
                  KOHA_BRANCH=${GITHUB_REF##*/}
                  echo "KOHA_BRANCH: $KOHA_BRANCH"
                  echo ::set-output name=KOHA_BRANCH::$KOHA_BRANCH
                  IFS='-' read -ra VER_PARTS <<< "$KOHA_BRANCH"
                  TMP_KOHA_BRANCH=${VER_PARTS[1]}
                  IFS='.' read -ra VER_PARTS <<< "$TMP_KOHA_BRANCH"
                  KOHA_MAJOR_MINOR="${VER_PARTS[0]}.${VER_PARTS[1]}"
                  echo "KOHA_MAJOR_MINOR: $KOHA_MAJOR_MINOR"
                  echo ::set-output name=KOHA_MAJOR_MINOR::$KOHA_MAJOR_MINOR
                  # Split the branch name into parts ( e.g. bywater-v19.05.01-03 => "bywater", "v19.05.01", "03 )
                  IFS='-' read -ra BRANCH_PARTS <<< "$KOHA_BRANCH"
                  # Extract shortname branch name
                  SHORTNAME=${BRANCH_PARTS[0]}
                  echo "SHORTNAME: $SHORTNAME"
                  echo ::set-output name=SHORTNAME::$SHORTNAME
                  # Extract version without v
                  TMP_VERSION=${BRANCH_PARTS[1]}
                  MMM_VERSION=${TMP_VERSION:1} # Removes the first character i.e. 'v19.05.01' => '19.05.01'
                  echo "MMM_VERSION: $MMM_VERSION"
                  echo ::set-output name=MMM_VERSION::$MMM_VERSION
                  # Extract mark
                  MARK=${BRANCH_PARTS[2]}
                  echo "MARK: $MARK"
                  echo ::set-output name=MARK::$MARK
                  # Build the branch specific part of the download path
                  DOWNLOAD_PATH="${KOHA_BRANCH}/koha-common_${MMM_VERSION}.${SHORTNAME}.${MARK}-1_all.deb"
                  echo ::set-output name=DOWNLOAD_PATH::$DOWNLOAD_PATH
                  echo "DOWNLOAD PATH: $DOWNLOAD_PATH"
                  # Set the base path of the URL, TODO: needs to change for bywater-koha-future
                  BASE_URL="https://github.com/${{github.repository}}/releases/download/"
                  DEB_URL="${BASE_URL}${DOWNLOAD_PATH}"
                  echo "DEB_URL: $DEB_URL"
                  echo ::set-output name=DEB_URL::$DEB_URL

            - name: Echo DEB_URL from previous step
              run: echo "${{ steps.extracted_variables.outputs.DEB_URL }}"

            - name: cd to koha-docker
              run: cd koha-docker

            - name: Current directory
              run: pwd

            - name: Show files
              run: ls -alh

            - name: Build the Docker image
              run: cd koha-docker; docker build --tag quay.io/bywatersolutions/koha:${{ steps.extracted_variables.outputs.SHORTNAME }}-${{ steps.extracted_variables.outputs.KOHA_MAJOR_MINOR }}-latest --tag quay.io/bywatersolutions/koha:${{ steps.extracted_variables.outputs.KOHA_BRANCH }} --build-arg KOHA_COMMON_DEB_URL=${{ steps.extracted_variables.outputs.DEB_URL }} --build-arg KOHA_VERSION=25.11 .

            - name: Log in to Docker Hub
              env:
                QUAY_USER: ${{secrets.QUAY_USER}}
                QUAY_PASSWORD: ${{secrets.QUAY_PASSWORD}}
              run: docker login quay.io -u $QUAY_USER -p $QUAY_PASSWORD

            - name: Push to Quay.io
              run: docker push quay.io/bywatersolutions/koha:${{ steps.extracted_variables.outputs.KOHA_BRANCH }}

            - name: Push to Quay.io - tag 'latest'
              run: docker push quay.io/bywatersolutions/koha:${{ steps.extracted_variables.outputs.SHORTNAME }}-${{ steps.extracted_variables.outputs.KOHA_MAJOR_MINOR }}-latest

    set-default-branch:
      name: Set default branch
      if: startsWith( github.ref, 'refs/heads/bywater-v' ) && github.repository == 'bywatersolutions/bywater-koha'
      needs: [build-debs, is-a-mark]
      runs-on: ubuntu-latest
      steps:
        - run: |
              KOHA_BRANCH=${GITHUB_REF##*/}
              echo "KOHA_BRANCH: $KOHA_BRANCH";
              gh repo edit bywatersolutions/bywater-koha --default-branch $KOHA_BRANCH
          env:
            GITHUB_TOKEN: ${{ secrets.OUR_GITHUB_TOKEN }}
            BRANCH: ${GITHUB_REF##*/}

    zap-scan-baseline-opac:
        name: ZAP Scan - Baseline - OPAC
        if: false
        needs: unit-test-perl
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v1

            - name: Export additional variables needed by koha-testing-docker
              run: |
                  export LOCAL_USER_ID="$(id -u)" # Needed for koha-testing-docker
                  KOHA_BRANCH=${GITHUB_REF##*/}
                  echo "BRANCH: $KOHA_BRANCH"
                  export SYNC_REPO=.
                  echo "LOCAL_USER_ID=$LOCAL_USER_ID" >> $GITHUB_ENV
                  echo "KOHA_BRANCH=$KOHA_BRANCH" >> $GITHUB_ENV
                  echo "SYNC_REPO=$SYNC_REPO" >> $GITHUB_ENV
                  echo "RUN_TESTS_AND_EXIT=yes" >> $GITHUB_ENV
                  echo "KOHA_IMAGE=25.11" >> $GITHUB_ENV

            - name: Set up koha-testing-docker
              run: |
                  sudo sysctl -w vm.max_map_count=262144
                  wget -O docker-compose.yml https://gitlab.com/koha-community/koha-testing-docker/raw/25.11/docker-compose.yml
                  mkdir -p env
                  wget -O env/defaults.env https://gitlab.com/koha-community/koha-testing-docker/raw/25.11/env/defaults.env
                  cp env/defaults.env .env
                  docker compose pull
                  docker compose -f docker-compose.yml -p koha up --detach
                  echo "SLEEPING 3 MINUTES"
                  sleep 60
                  echo "1 MINUTE DONE"
                  sleep 60
                  echo "2 MINUTES DONE"
                  sleep 60
                  echo "3 MINUTES DONE"
                  echo "WAKING UP"
                  echo "DOCKER LOGS"
                  docker logs koha-koha-1 2>&1
                  echo "/DOCKER LOGS"

            #    - name: Setup Debug Session
            #      uses: csexton/debugger-action@master

            - name: List docker networks
              run: |
                  docker network ls

            - name: Scan opac interface
              run: |
                  docker run --network koha_kohanet -t owasp/zap2docker-weekly zap-baseline.py -t http://koha:8080

            - name: Post test cleanup
              run: |
                  docker compose down
                  docker rm -f $(docker ps -a -f "name=koha_" -q)
                  docker volume prune -f
                  docker image  prune -f
                  rm docker-compose.yml
                  rm -rf env .env

            - uses: bywatersolutions/action-slack@v3
              with:
                status: ${{ job.status }}
                fields: ref,workflow # selectable (default: repo,message)
              env:
                SLACK_WEBHOOK_URL: ${{ secrets.SLACK_DEVOPS_WEBHOOK }} # required
              if: failure() # Only notify on job failures.

    zap-scan-baseline-staff:
        name: ZAP Scan - Baseline - Staff
        if: false
        needs: unit-test-perl
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v1

            - name: Export additional variables needed by koha-testing-docker
              run: |
                  export LOCAL_USER_ID="$(id -u)" # Needed for koha-testing-docker
                  KOHA_BRANCH=${GITHUB_REF##*/}
                  echo "BRANCH: $KOHA_BRANCH"
                  export SYNC_REPO=.
                  echo "LOCAL_USER_ID=$LOCAL_USER_ID" >> $GITHUB_ENV
                  echo "KOHA_BRANCH=$KOHA_BRANCH" >> $GITHUB_ENV
                  echo "SYNC_REPO=$SYNC_REPO" >> $GITHUB_ENV
                  echo "RUN_TESTS_AND_EXIT=yes" >> $GITHUB_ENV
                  echo "KOHA_IMAGE=25.11" >> $GITHUB_ENV

            - name: Set up koha-testing-docker
              run: |
                  sudo sysctl -w vm.max_map_count=262144
                  wget -O docker-compose.yml https://gitlab.com/koha-community/koha-testing-docker/raw/25.11/docker-compose.yml
                  mkdir -p env
                  wget -O env/defaults.env https://gitlab.com/koha-community/koha-testing-docker/raw/25.11/env/defaults.env
                  cp env/defaults.env .env
                  docker compose pull
                  docker compose -f docker-compose.yml -p koha up --detach
                  echo "SLEEPING 3 MINUTES"
                  sleep 60
                  echo "1 MINUTE DONE"
                  sleep 60
                  echo "2 MINUTES DONE"
                  sleep 60
                  echo "3 MINUTES DONE"
                  echo "WAKING UP"
                  echo "DOCKER LOGS"
                  docker logs koha-koha-1 2>&1
                  echo "/DOCKER LOGS"

            #    - name: Setup Debug Session
            #      uses: csexton/debugger-action@master

            - name: List docker networks
              run: |
                  docker network ls

            - name: Scan staff interface
              run: |
                  docker run --network koha_kohanet -t owasp/zap2docker-weekly zap-baseline.py -t http://koha:8081

            - name: Post test cleanup
              run: |
                  docker compose down
                  docker rm -f $(docker ps -a -f "name=koha_" -q)
                  docker volume prune -f
                  docker image  prune -f
                  rm docker-compose.yml
                  rm -rf env .env

            - uses: bywatersolutions/action-slack@v3
              with:
                status: ${{ job.status }}
                fields: ref,workflow # selectable (default: repo,message)
              env:
                SLACK_WEBHOOK_URL: ${{ secrets.SLACK_DEVOPS_WEBHOOK }} # required
              if: failure() # Only notify on job failures.

    zap-scan-full-staff:
        name: ZAP Scan - Full - Staff
        if: false
        needs: zap-scan-baseline-staff
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v1

            - name: Export additional variables needed by koha-testing-docker
              run: |
                  export LOCAL_USER_ID="$(id -u)" # Needed for koha-testing-docker
                  KOHA_BRANCH=${GITHUB_REF##*/}
                  echo "BRANCH: $KOHA_BRANCH"
                  export SYNC_REPO=.
                  echo "LOCAL_USER_ID=$LOCAL_USER_ID" >> $GITHUB_ENV
                  echo "KOHA_BRANCH=$KOHA_BRANCH" >> $GITHUB_ENV
                  echo "SYNC_REPO=$SYNC_REPO" >> $GITHUB_ENV
                  echo "RUN_TESTS_AND_EXIT=yes" >> $GITHUB_ENV
                  echo "KOHA_IMAGE=25.11" >> $GITHUB_ENV

            - name: Set up koha-testing-docker
              run: |
                  sudo sysctl -w vm.max_map_count=262144
                  wget -O docker-compose.yml https://gitlab.com/koha-community/koha-testing-docker/raw/25.11/docker-compose.yml
                  mkdir -p env
                  wget -O env/defaults.env https://gitlab.com/koha-community/koha-testing-docker/raw/25.11/env/defaults.env
                  cp env/defaults.env .env
                  docker compose pull
                  docker compose -f docker-compose.yml -p koha up --detach
                  echo "SLEEPING 3 MINUTES"
                  sleep 60
                  echo "1 MINUTE DONE"
                  sleep 60
                  echo "2 MINUTES DONE"
                  sleep 60
                  echo "3 MINUTES DONE"
                  echo "WAKING UP"
                  echo "DOCKER LOGS"
                  docker logs koha-koha-1 2>&1
                  echo "/DOCKER LOGS"

            #    - name: Setup Debug Session
            #      uses: csexton/debugger-action@master

            - name: List docker networks
              run: |
                  docker network ls

            - name: Scan staff interface
              run: |
                  docker run --network koha_kohanet -t owasp/zap2docker-weekly zap-full-scan.py -t http://koha:8081

            - name: Post test cleanup
              run: |
                  docker compose down
                  docker rm -f $(docker ps -a -f "name=koha_" -q)
                  docker volume prune -f
                  docker image  prune -f
                  rm docker-compose.yml
                  rm -rf env .env

            - uses: bywatersolutions/action-slack@v3
              with:
                status: ${{ job.status }}
                fields: ref,workflow # selectable (default: repo,message)
              env:
                SLACK_WEBHOOK_URL: ${{ secrets.SLACK_DEVOPS_WEBHOOK }} # required
              if: failure() # Only notify on job failures.

    zap-scan-full-opac:
        name: ZAP Scan - Full - OPAC
        if: false
        needs: zap-scan-baseline-opac
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v1

            - name: Export additional variables needed by koha-testing-docker
              run: |
                  export LOCAL_USER_ID="$(id -u)" # Needed for koha-testing-docker
                  KOHA_BRANCH=${GITHUB_REF##*/}
                  echo "BRANCH: $KOHA_BRANCH"
                  export SYNC_REPO=.
                  echo "LOCAL_USER_ID=$LOCAL_USER_ID" >> $GITHUB_ENV
                  echo "KOHA_BRANCH=$KOHA_BRANCH" >> $GITHUB_ENV
                  echo "SYNC_REPO=$SYNC_REPO" >> $GITHUB_ENV
                  echo "RUN_TESTS_AND_EXIT=yes" >> $GITHUB_ENV
                  echo "KOHA_IMAGE=25.11" >> $GITHUB_ENV

            - name: Set up koha-testing-docker
              run: |
                  sudo sysctl -w vm.max_map_count=262144
                  wget -O docker-compose.yml https://gitlab.com/koha-community/koha-testing-docker/raw/25.11/docker-compose.yml
                  mkdir -p env
                  wget -O env/defaults.env https://gitlab.com/koha-community/koha-testing-docker/raw/25.11/env/defaults.env
                  cp env/defaults.env .env
                  docker compose pull
                  docker compose -f docker-compose.yml -p koha up --detach
                  echo "SLEEPING 3 MINUTES"
                  sleep 60
                  echo "1 MINUTE DONE"
                  sleep 60
                  echo "2 MINUTES DONE"
                  sleep 60
                  echo "3 MINUTES DONE"
                  echo "WAKING UP"
                  echo "DOCKER LOGS"
                  docker logs koha-koha-1 2>&1
                  echo "/DOCKER LOGS"

            #    - name: Setup Debug Session
            #      uses: csexton/debugger-action@master

            - name: List docker networks
              run: |
                  docker network ls

            - name: Scan opac interface
              run: |
                  docker run --network koha_kohanet -t owasp/zap2docker-weekly zap-full-scan.py -t http://koha:8080

            - name: Post test cleanup
              run: |
                  docker compose down
                  docker volume prune -f
                  docker image  prune -f
                  rm docker-compose.yml
                  rm -rf env .env

            - uses: bywatersolutions/action-slack@v3
              with:
                status: ${{ job.status }}
                fields: ref,workflow # selectable (default: repo,message)
              env:
                SLACK_WEBHOOK_URL: ${{ secrets.SLACK_DEVOPS_WEBHOOK }} # required
              if: failure() # Only notify on job failures.

    auto-rebase-xslt:
        if: false
        #if: startsWith( github.ref, 'refs/heads/bywater-v' )
        name: Rebase XSLT
        needs: [unit-test-perl, is-a-mark]
        runs-on: ubuntu-latest
        strategy:
            fail-fast: false
            matrix:
                branch:
                    - ajaxon
                    - bedford
                    - bedtest
                    - briar
                    - camden
                    - centenary
                    - duncanville
                    - esripsa
                    - fredh
                    - keys
                    - library
                    - nrhtx
                    - nyam
                    - nycrecords
                    - ottumwa
                    - palnet
                    - panhandle
                    - presby
                    - rosev
                    - rta
                    - siskiyou
                    - taft
                    - vatech
                    - vatechtest
                    - arlington

        steps:
            - uses: actions/checkout@v1

            - name: Display xslt branch to be rebased
              run: echo ${{ matrix.branch }}

            - name: Add xslt remote for bywater-koha-xslt
              run: git remote add bywater-koha-xslt https://github.com/bywatersolutions/bywater-koha-xslt.git

            - name: Add xslt remote for bywater-koha-xslt-archives
              run: git remote add bywater-koha-xslt https://github.com/bywatersolutions/bywater-koha-xslt-archives.git

            - name: Fetch from xslt remotes
              run: git fetch --all

            - name: Check out the xslt branch to be rebassed
              run: git checkout bywater-koha-xslt/${{ matrix.branch }}

            - name: Push a backup copy of this branch to bywater-koha-xslt
              run: git push bywater-koha-xslt-archives HEAD:refs/heads/${GITHUB_REF##*/}-$(date --iso-8601)

            - name: Get first and last commits of custom XSLT
              id: extracted_variables
              run: |
                  FIRST_COMMIT=$(git log --all --grep='BWS-XSLT' --pretty=format:"%H" | tail -1)
                  echo "FIRST COMMIT: $FIRST_COMMIT"
                  echo ::set-output name=FIRST_COMMIT::$FIRST_COMMIT
                  LAST_COMMIT=$(git log --all --grep='BWS-XSLT' --pretty=format:"%H" | head -1)
                  echo "LAST COMMIT: $LAST_COMMIT"
                  echo ::set-output name=LAST_COMMIT::$LAST_COMMIT

            - name: Check out the base Koha branch to cherry pick the XSLT commits on to
              run: git checkout ${{ github.sha}}

            - name: Cherry pick the commits
              run: git cherry-pick ${{ steps.extracted_variables.outputs.LAST_COMMIT }}^..${{ steps.extracted_variables.outputs.FIRST_COMMIT }}

            - name: Push the rebased branch back
              run: git push -f bywater-koha-xslt HEAD:${{ github.ref }}
